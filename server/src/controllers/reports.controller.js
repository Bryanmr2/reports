const createReport = () => {
    // Logica de transformar los datos en el reporte, 
    // let insertResult = createReportInDB(req.body)
    // buildPDFReport()

    
};

module.exports = {
    createReport
}